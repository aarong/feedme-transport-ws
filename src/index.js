import client from "./client";
import browser from "./browser";
import server from "./server";
/**
 * Common entry point for server, client, and browser modules.
 * @returns {Object}
 */
export default {
  client,
  browser,
  server
};
